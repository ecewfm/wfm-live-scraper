// lib/version.js
// Bump BUILD (and update NOTE) any time a meaningful fix/feature ships.
// Shown in the terminal dashboard's header line (always visible, doesn't
// scroll away with the log) so it's easy to confirm a restart actually
// picked up the latest code, instead of guessing from behavior alone.

module.exports = {
  BUILD: '2026-09-07.1',
  NOTE:  '7cs-live: Aircall "Live Monitoring +" redesign fix — KPI tile values, Users/Calls tab-switch, agent status rebuild',
}
